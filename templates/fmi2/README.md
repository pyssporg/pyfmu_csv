# FMI 2.0 Templates

This directory is reserved for reusable FMI 2.0 packaging templates.

The initial scaffold keeps XML generation in Python so the layout is easy to evolve. As the wire format stabilizes, reusable template assets can live here without changing the reusable C++ runtime boundary.
